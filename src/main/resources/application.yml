spring:
  application:
    name: cliente-core
  profiles:
    active: dev

server:
  port: 8081
  servlet:
    context-path: /api/clientes

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: when-authorized