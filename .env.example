# ============================================================================
# VARIÁVEIS DE AMBIENTE - DESENVOLVIMENTO LOCAL
# ============================================================================
#
# INSTRUÇÕES:
# 1. Copie este arquivo para .env:
#    cp .env.example .env
#
# 2. NÃO commite o arquivo .env (já está no .gitignore)
#
# 3. Configure as variáveis abaixo conforme seu ambiente local
#
# 4. O sistema carregará automaticamente estas variáveis ao rodar mvn spring-boot:run
#
# ============================================================================

# Perfil do Spring Boot (dev = sem OAuth2, prod = com OAuth2)
SPRING_PROFILES_ACTIVE=dev

# Database PostgreSQL local
SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5432/clientes
SPRING_DATASOURCE_USERNAME=postgres
SPRING_DATASOURCE_PASSWORD=postgres

# Porta da aplicação (8081 é o padrão)
SERVER_PORT=8081

# Log level (DEBUG para dev, INFO para prod)
LOGGING_LEVEL_ROOT=INFO
LOGGING_LEVEL_BR_COM_VANESSA_MUDANCA=DEBUG
